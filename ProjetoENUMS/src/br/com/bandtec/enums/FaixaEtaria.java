//o pacote chama enums pq enum é palavra reservada
package br.com.bandtec.enums;

// criado como classe normal, renomeado como enum
public enum FaixaEtaria {
    
    //por convenção, os enum são em caixa alta - separados por virgula e o ultimo com ; ponto-e-virgula
    //a ordem dos elementos do enum não altera a execução
    INFANCIA,
    ADOLESCENCIA,
    ADULTO,
    IDOSO;
    
}
